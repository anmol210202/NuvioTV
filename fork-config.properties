# NuvioTV Lite Fork Configuration

# Lite Mode overrides
LITE_MODE=true
BLUR_ENABLED=false
TRAILER_PREVIEW_ENABLED=false
MPV_ENABLED=false
ANIMATED_IMAGE_DECODER_ENABLED=false

# Cache Sizes (MB)
IMAGE_DISK_CACHE_SIZE_MB=80
